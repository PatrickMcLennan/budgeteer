import styled from 'styled-components';

export const StyledNav = styled.nav`
  position: fixed;
  top: 5vh;
  left: 50%;
  transform: translateX(-50%);
  padding: 1vh 0;
  width: 100%;
  text-align: center;
  z-index: 10;
`;
