import styled from 'styled-components';

export const StyledNav = styled.nav`
  padding: 1vh 0;
  grid-area: 1 / 1 / 2 / -1;
  text-align: center;
  position: fixed;
  left: 50%;
  transform: translateX(-50%);
  z-index: 10;
`;
