import styled from 'styled-components';

export const StyledNav = styled.nav`
  /* /* position: fixed; */
  z-index: 10;
  grid-area: 1 / 1 / 2 / -1;
  padding: 1vh 0;
  width: 100%;
  text-align: center;
`;
